<%= form_for current_user.relationships.find_by_followed_id(@user),
             :html => { :method => :delete },
             :remote => true do |f| %>
  <div class="follow_main"><%= f.submit '', :class => 'unfollow_button' %></div>
<% end %>