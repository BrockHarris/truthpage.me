<% if @fbfeed_items.empty? %>
  <div class="align-text">
    fb friends havent shared anything.
  </div>
<% else %>
  <table class="fb_feed">
    <%= render :partial => 'shared/fbfeed_item', :collection => @fbfeed_items %>
  </table>
<% end %>