<%= form_for @micropost do |f| %>  
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field" >
		<%= f.text_area :content, :placeholder =>"truth is...", :maxlength=>"180", :id =>"truth_form" %>
  </div>
  <div class="field">
		<%= f.hidden_field :belongs_to_id, :value => @user.id %>
  </div>
	<div class="anon_button">
		<%= f.check_box :anon, {}, true, false %>
		<%= f.label "share anonymously" %>
	</div>
  <div class ="share_placement">
  	<% if current_user %>
  		<%= f.submit 'Send', :class => 'submitpost', :id=> 'load_animate'%>
  	<% else %>
  		<%= link_to 'Send', login_path, :id => 'authenticate', :class => 'submitpost', :style=>'color:#FFFFFF;' %>
  	<% end %>
  </div>
<% end %>

